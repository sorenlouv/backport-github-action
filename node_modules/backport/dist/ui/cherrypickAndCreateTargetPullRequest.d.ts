import { BackportOptions } from '../options/options';
import { CommitSelected } from '../types/Commit';
export declare function cherrypickAndCreateTargetPullRequest({ options, commits, targetBranch, }: {
    options: BackportOptions;
    commits: CommitSelected[];
    targetBranch: string;
}): Promise<{
    url: string;
    number: number;
}>;
export declare function getBackportBranchName(targetBranch: string, commits: CommitSelected[]): string;
