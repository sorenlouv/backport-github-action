import { BackportOptions } from '../../../options/options';
import { CommitSelected } from '../../../types/Commit';
export interface PullRequestPayload {
    owner: string;
    repo: string;
    title: string;
    body: string;
    head: string;
    base: string;
    [key: string]: unknown;
}
export declare function createPullRequest({ options, prPayload, }: {
    options: BackportOptions;
    prPayload: PullRequestPayload;
}): Promise<{
    url: string;
    number: number;
}>;
export declare function getBody({ options, commits, targetBranch, }: {
    options: BackportOptions;
    commits: CommitSelected[];
    targetBranch: string;
}): string;
export declare function getTitle({ options, commits, targetBranch, }: {
    options: BackportOptions;
    commits: CommitSelected[];
    targetBranch: string;
}): string;
