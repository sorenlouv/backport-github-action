import { BackportOptions } from '../../../options/options';
export declare function addAssigneesToPullRequest({ githubApiBaseUrlV3, repoName, repoOwner, accessToken, username, dryRun, }: BackportOptions, pullNumber: number, assignees: string[]): Promise<void>;
