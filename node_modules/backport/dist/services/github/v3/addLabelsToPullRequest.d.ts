import { BackportOptions } from '../../../options/options';
export declare function addLabelsToPullRequest({ githubApiBaseUrlV3, repoName, repoOwner, accessToken, dryRun, }: BackportOptions, pullNumber: number, labels: string[]): Promise<void>;
