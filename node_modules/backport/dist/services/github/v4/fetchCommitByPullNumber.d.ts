import { BackportOptions } from '../../../options/options';
import { CommitSelected } from '../../../types/Commit';
export declare function fetchCommitByPullNumber(options: BackportOptions & {
    pullNumber: number;
}): Promise<CommitSelected>;
