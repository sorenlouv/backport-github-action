import { ValidatedOptions } from '../../../options/getValidatedOptions';
export interface DataResponse {
    viewer: {
        login: string;
    };
    repository: {
        ref?: {
            name: string;
        };
        defaultBranchRef: {
            name: string;
        };
    };
}
export declare function fetchDefaultRepoBranchAndPerformStartupChecks({ accessToken, githubApiBaseUrlV4, repoName, repoOwner, }: ValidatedOptions): Promise<{
    defaultBranch: string;
}>;
