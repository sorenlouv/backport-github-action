import { ValidatedOptions } from '../../../options/getValidatedOptions';
export interface DataResponse {
    repository: {
        ref?: {
            name: string;
        };
        defaultBranchRef: {
            name: string;
        };
    };
}
export declare function getDefaultRepoBranchAndPerformStartupChecks({ accessToken, githubApiBaseUrlV4, repoName, repoOwner, }: ValidatedOptions): Promise<{
    defaultBranch: string;
}>;
