export declare function getBranches(cwd: string): Promise<string[]>;
export declare function getLatestCommit({ branch, commitCount, cwd, }: {
    branch: string;
    commitCount: number;
    cwd: string;
}): Promise<string>;
export declare function deleteAndSetupEnvironment(): Promise<void>;
