import * as Octokit from "@octokit/rest";
declare const cherryPickCommits: ({ _intercept, commits, head, octokit, owner, repo, }: {
    _intercept?: (({ initialHeadSha }: {
        initialHeadSha: string;
    }) => Promise<void>) | undefined;
    commits: string[];
    head: string;
    octokit: Octokit;
    owner: string;
    repo: string;
}) => Promise<string>;
export { cherryPickCommits };
